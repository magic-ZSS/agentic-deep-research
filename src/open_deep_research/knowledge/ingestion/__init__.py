"""Local candidate ingestion package.

Import concrete contracts from ``ingestion.models`` or ``ingestion.service``.
Keeping this module deliberately lazy prevents a Repository/model import cycle.
"""
